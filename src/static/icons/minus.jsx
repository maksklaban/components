import React from 'react'

export const minus = (props) => (
  <svg width={props.width || '6'} height={props.height || '2'} viewBox="0 0 6 2">
    <rect width={props.width || '6'} height={props.height || '2'} fill={props.fill || '#black'} />
  </svg>
)
